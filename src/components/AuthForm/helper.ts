import {TAuthFormFields} from '~/components/AuthForm/types.ts';
import RestService from '~/services/RestService.ts';

const validateAuthForm = (fields: TAuthFormFields) => {
  let errors = {};
  return errors;
};

export const submitAuthForm = async (fields: TAuthFormFields, registration: boolean) => {

};
